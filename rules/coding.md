# Coding Rules — Dashboard

- Static HTML only — no build tools, no npm, no frameworks
- CSS inline or in `<style>` blocks
- JavaScript inline or in `<script>` blocks
- Airbnb-style design: light theme, generous whitespace, subtle shadows, card-based layout
- Mobile responsive
- Links must be clickable
- When updating: change ONLY the affected section, not the whole file
